import { FilterTeasByMinPricePipe } from './filter-teas-by-min-price.pipe';

describe('FilterTeasByMinPricePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTeasByMinPricePipe();
    expect(pipe).toBeTruthy();
  });
});
