import { FilterTeasByCharacteristicsPipe } from './filter-teas-by-characteristics.pipe';

describe('FilterTeasByCharacteristicsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTeasByCharacteristicsPipe();
    expect(pipe).toBeTruthy();
  });
});
