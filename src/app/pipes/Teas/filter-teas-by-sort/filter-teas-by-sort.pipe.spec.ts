import { FilterTeasBySortPipe } from './filter-teas-by-sort.pipe';

describe('FilterTeasBySortPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTeasBySortPipe();
    expect(pipe).toBeTruthy();
  });
});
