import { FilterTeasByAromasPipe } from './filter-teas-by-aromas.pipe';

describe('FilterTeasByAromasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTeasByAromasPipe();
    expect(pipe).toBeTruthy();
  });
});
