import {FilterOfferTeasPipe} from './filter-offer-teas.pipe';

describe('FilterOfferTeasPipe', () => {
    it('create an instance', () => {
        const pipe = new FilterOfferTeasPipe();
        expect(pipe).toBeTruthy();
    });
    it('should filter offer teas', () => {

    });
});
