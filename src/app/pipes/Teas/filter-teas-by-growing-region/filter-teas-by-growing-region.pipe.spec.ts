import { FilterTeasByGrowingRegionPipe } from './filter-teas-by-growing-region.pipe';

describe('FilterTeasByGrowingRegionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTeasByGrowingRegionPipe();
    expect(pipe).toBeTruthy();
  });
});
